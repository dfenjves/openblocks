<h1><%= @dashboard.name %></h1>
	<% @dashboard.spots.each do |spot| %>
	<a href="<%=spot.website%>">
		<div>
			<h3><%=spot.name%></h3>
			</br>
			<h2 class="<%=spot.current_status%>"><%=spot.current_status%></h2>
			<br>
			<p><%=spot.today_schedule%></p>
			<!-- <p><%#= link_to '✖', delete_dashboard_spot_path(@dashboard, spot), remote: true, class: 'remove', method: :delete %></p> -->
		</div>
	</a>
	<% end %>
	<a href="#">
		<div>
			<h2 id="add_spot">+</h2>
		</div>
	</a>
